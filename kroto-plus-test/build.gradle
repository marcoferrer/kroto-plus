description = "Kroto+ Test Support"

apply from: "$rootDir/publishing.gradle"

dependencies {
    implementation "io.grpc:grpc-testing:${Versions.grpc}"
    implementation "com.google.protobuf:protobuf-java:${Versions.protobuf}"
    implementation project(":kroto-plus-message")
}