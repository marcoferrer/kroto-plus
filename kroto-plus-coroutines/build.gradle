description = "Kroto+ Coroutine Support"

dependencies {
    implementation "org.jetbrains.kotlinx:kotlinx-coroutines-core:${versions.coroutines}"
    implementation "io.grpc:grpc-protobuf:${versions.grpc}",
                   "io.grpc:grpc-stub:${versions.grpc}"
}